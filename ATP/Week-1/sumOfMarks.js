// Finding the sum of all marks using an array
let marks = [90, 78, 65, 98];
let sum = 0;

// Loop through array and add values
for (let i = 0; i < marks.length; i++) {
    sum = sum + marks[i];
}

console.log("Sum of marks:", sum);
